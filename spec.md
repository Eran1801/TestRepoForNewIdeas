# ממצגת למשימות Jira

אפיון מוצר · תהליך גנרי

> **מסמך זה עודכן בהתאם לכל ההחלטות שהתקבלו בשיחה — ראו "היסטוריית עדכונים" בתחתית.**

מערכת גנרית שהופכת מצגת אפיון (PowerPoint) לרשימת משימות מוכנות ב-Jira — עם **הצלבה מול קוד קיים**, שיוך ל-Board ולעובד הנכון, ואישור לפני יצירה של כל משימה.

## למה

אפיונים מגיעים כמצגת PowerPoint, בלי חיבור לסטטוס האמיתי של הקוד. התוצאה: משימות ב-Jira עבור דברים שכבר בוצעו, אפיון שלא ממופה נכון למבנה הפרויקט (Epic, Board, אחראים), ויצירת כל טיקט בנפרד באופן ידני.

## שלבי התהליך

### 1. קליטת מצגת

המערכת מקבלת מצגת PowerPoint של האפיון, וממירה אותה לקובץ Markdown באמצעות הספרייה `markitdown` של מיקרוסופט.

### 2. ניתוח והצלבה מול קוד

קובץ ה-md מנותח ומוצלב מול ה-source code הקיים במערכת — כדי לזהות אילו חלקים מהאפיון כבר מומשו בפועל, ולא להסתמך רק על טקסט המצגת. הניתוח גם קובע אם דרישה נוגעת לצד שרת, לצד לקוח, או לשניהם — **ודרישה שנוגעת לשניהם הופכת כבר בשלב הזה לשתי משימות נפרדות, אחת לכל צד** (לא משימה אחת עם תתי-משימות — ראו שלב 6).

### 3. חיבור ל-Jira

המערכת מבקשת לינק ל-Jira של הפרויקט (Board). מהלינק הזה נשאבים המשתמשים וה-Epic-ים הקיימים, שישמשו בהמשך לשיוך המשימות.

### 4. יצירת משימות ולוח קוביות

בהתאם לניתוח — כולל מה שכבר בוצע — המערכת יוצרת רשימת משימות ומציגה אותן כלוח קוביות. במעלה המסך נבחרים אחראי ו-Epic מתוך האופציות שנשאבו מה-Board.

כל קוביה כוללת:
- כותרת ותיאור, ניתנים לעריכה ישירה בקוביה
- כפתור **הרחב** שפותח חלון גדול יותר לעריכה נוחה של הכותרת והתיאור
- מספר העמוד באפיון שממנו נגזרה המשימה
- ציטוט מהטקסט המקורי בעמוד — **תמיד מוצג** (לא אופציונלי), כדי שמנהל הפרויקט יראה מה בדיוק היה כתוב באפיון ויוכל לדייק את המשימה בהתאם
- תגית היקף: **צד שרת או צד לקוח** — כל קוביה משויכת לצד אחד בלבד, אף פעם לא "שניהם"

### 5. יצירה ב-Jira

בכל קוביה יש כפתור **יצירה** שפותח את המשימה ב-Jira תחת `To Do`, תמיד מסוג Issue `Task` — לא Story, Bug או סוג אחר. יעד היצירה (עמודה / סטטוס) ניתן להגדרה גלובלית לכל המשימות, או בנפרד לכל משימה.

### 6. משימות ותתי-משימות (יכולת נפרדת)

קיימת גם אפשרות ליצור משימה אחת גדולה עם תתי-משימות, כשמשימה בודדת מורכבת מספיק שכדאי לפרק אותה — למשל כמה שלבי מימוש בתוך אותו צד.

**זה נפרד לגמרי מפיצול שרת/לקוח**: משימה שנוגעת גם לשרת וגם ללקוח היא תמיד שתי משימות עצמאיות (שלב 2) — לא משימה אחת עם תתי-משימות. לכל אחת כפתור יצירה, סטטוס והיסטוריה משלה.

כשכן נעשה שימוש בתתי-משימות (למקרה הכללי, לא לפיצול שרת/לקוח), המשימה הראשית מסוג `Task` ותתי-המשימות מסוג `Sub-task` — סוג ה-Issue הייעודי של Jira לפריטים תחת משימת-על.

## הסטאק

- **markitdown** — הופך את מצגת ה-PowerPoint לקובץ Markdown. הספרייה היא Python בלבד — אם ה-backend הראשי הוא PHP, השלב הזה רץ כתהליך/שירות Python נפרד, לא מיושם מחדש ב-PHP
- **Backend — PHP או Python**, לפי הפרויקט הקיים שהמערכת נשתלת בתוכו. מריץ את הניתוח מול הקוד (קריאת API למודל LLM), את יצירת המשימות, ואת כל התקשורת מול Jira
- **Jira REST API (v3), לא MCP** — שאיבת Board / Epic / Users, יצירת משימות ותתי-משימות, עדכון סטטוס — ישירות מה-backend הקיים. MCP מיועד לחיבור סוכן-LLM לכלים בתוך סשן אינטראקטיבי, לא לאינטגרציה קבועה בתוך פרויקט קיים
- **אימות מול Jira** — API token מספיק לארגון פנימי אחד; OAuth 2.0 (3LO) אם כמה ארגונים או משתמשים מתחברים כל אחד לחשבון Jira שלו
- **Frontend — React** — לוח הקוביות נבנה כ-bundle נפרד (למשל עם Vite) ומוטמע בעמוד בפרויקט הקיים דרך `<div id="root">`, כולל אם ה-backend הוא PHP; התקשורת מול השרת היא קריאות API רגילות

## עקרונות מפתח

- **אישור לפני יצירה.** לוח הקוביות הוא טיוטה לבדיקה — משימה נוצרת ב-Jira רק בלחיצה מפורשת, לא אוטומטית.
- **לזהות מה כבר בוצע.** הצלבה מול הקוד הקיים מונעת כפילות של משימות שכבר מומשו.
- **לשאוב Epic ומשתמשים מה-Board עצמו,** לא לנחש — כדי שהשיוך יהיה נכון מההתחלה.
- **שרת ולקוח הן תמיד שתי משימות נפרדות,** לא משימת-על עם תתי-משימות — כל אחת עם מעקב, סטטוס והיסטוריה עצמאיים ב-Jira.

## נתוני ייחוס (מהאב-טיפוס)

ערכים אמיתיים שאומתו באב-הטיפוס האינטראקטיבי, לשימוש כברירת מחדל ב-build האמיתי:

- **משתמשים:** אור שמש · ערן לוי · דור עזריה · יוליה · רביב לנקרי
- **סטטוסים (רישיות מלאות):** `TO DO` · `IN PROGRESS` · `DONE` · `READY FOR QA` · `WAITING FOR IBI` · `DONE DONE`
- **Epics:** No Epic · Inbox · Profile · More Services · Equity Holders · Cap Table · Reports

## שפה משותפת (CONTEXT.md)

`CONTEXT.md` בריפו הזה מחזיק את המילון של הפרויקט וההחלטות (ADRs). הוא נכתב כדי לפתור התנגשות אמיתית: המילה "spec" שימשה אצלנו גם לקלט (**אפיון** — מצגת ה-PowerPoint שהמערכת קוראת) וגם לפלט התכנון שלנו (**product spec** — המסמך הזה). מעכשיו הן שני מונחים נפרדים. קראו את `CONTEXT.md` לפני שכותבים קוד או טיקטים.

## סקילים שאנחנו משתמשים בהם

מקור: **https://github.com/mattpocock/skills** — אוסף סקילים לסוכני AI לעבודת הנדסה אמיתית (לא vibe coding). התקנה: `claude plugins install mattpocock-skills`, או `npx skills@latest add mattpocock/skills`.

הרעיון המרכזי שרלוונטי לנו: הסקילים מתוכננים להיות **קטנים, ניתנים לעריכה ומורכבים זה על זה**, במקום framework שמשתלט על התהליך. זה בדיוק המודל שאנחנו רוצים למערכת שלנו.

לפני שימוש ראשון בריפו: `/setup-matt-pocock-skills` (מגדיר issue tracker, תוויות triage, ומיקום מסמכים).

### הסקילים שאנחנו צריכים

| סקיל | למה דווקא אצלנו |
|---|---|
| **`grill-with-docs`** | הסקיל הכי חשוב עבורנו. תשאול חוזר עד שכל ענפי עץ ההחלטות סגורים, ותוך כדי בונה מילון ו-ADRs. יש לנו ארבע שאלות פתוחות ב-`CONTEXT.md` שחייבות תשובה לפני שכותבים שורת קוד — במיוחד **איך ה"הצלבה" באמת מחליטה שמשהו כבר קיים בקוד**, שהיא הטענה המרכזית של המוצר וגם הבעיה ההנדסית הקשה ביותר. |
| **`to-tickets`** | מפרק אפיון לטיקטים עם **תלויות מפורשות** (blocking edges) ובגודל שנכנס לחלון קונטקסט אחד. זה כמעט בדיוק מה שהמוצר שלנו עושה — ראו "מה למדנו" למטה. משמש אותנו גם ככלי לפירוק ה-build שלנו וגם כמקור השראה לעיצוב המוצר. |
| **`domain-modeling`** | כבר הופעל — הוא שהפיק את `CONTEXT.md`. ימשיך לשמש בכל פעם שנוסיף מונח או החלטה. |
| **`tdd`** | מנוע הניתוח הוא הלב של המוצר ואי אפשר לבדוק אותו בעין: פלט LLM, פרסינג של Markdown, והצלבה מול קוד. red-green-refactor הוא הדרך היחידה לדעת שהוא לא נשבר. |
| **`research`** | לפני מימוש נצטרך עובדות מדויקות ממקורות ראשוניים: התנהגות `markitdown` על PPTX עברי, שדות חובה ב-Jira REST v3, ומה בדיוק OAuth 3LO דורש. הסקיל שומר את הממצאים כקובץ Markdown מצוטט בריפו. |
| **`wizard`** | הגדרת ההרשאות מול Jira (API token או OAuth 3LO, הרשאות ב-Board) היא בדיוק "צעדים שרק אדם יכול לבצע". הסקיל מייצר אשף bash שמוביל את המשתמש דרכם. |
| **`code-review`** | ביקורת דו-צירית: גם מול תקני הקוד של הריפו וגם **מול האפיון המקורי** — האם הקוד באמת מממש את מה שהטיקט ביקש. מתאים למוצר שכל כולו עוסק בנאמנות לאפיון. |
| **`diagnosing-bugs`** | לולאת דיבוג ממושמעת. רלוונטי כי הבאגים הצפויים אצלנו הם מהסוג הקשה: פלט לא דטרמיניסטי ממודל, וקידוד/כיווניות עברית. |
| **`prototype`** | כבר עשינו את זה בפועל (ה-HTML הבודד). הסקיל מגדיר את הכללים: קוד לזריקה, בלי persistence, ולתפוס את המסקנה כשמסיימים. |
| **`implement`** | בונה מתוך טיקטים, מפעיל `tdd` בתפרים מוסכמים ומסיים ב-`code-review`. זה השלב שאחרי `to-tickets`. |

### מה למדנו מהריפו על המוצר שלנו

`to-tickets` הוא בעצם המוצר שלנו, בכתב. שווה לאמץ ממנו שלושה דברים:

1. **תלויות בין משימות (blocking edges).** כל טיקט מצהיר אילו טיקטים חוסמים אותו. אצלנו אין את זה בכלל — אנחנו מייצרים רשימה שטוחה. זו כנראה התוספת בעלת הערך הגבוה ביותר למוצר: מנהל פרויקט צריך לדעת מה אפשר להתחיל עכשיו.
2. **גודל טיקט = חלון קונטקסט אחד.** קריטריון חד לגודל משימה, שאפשר לאמץ כהנחיה למנוע הניתוח.
3. **⚠️ פרוסות אנכיות מול אופקיות.** הסקיל טוען שטיקט צריך לחתוך **פרוסה אנכית** — נתיב שלם דרך כל השכבות, שניתן להדגים לבד — ולא פרוסה של שכבה אחת. **הפיצול שרת/לקוח שלנו הוא פרוסה אופקית**, כלומר בדיוק מה שהוא ממליץ נגדו. זו לא בהכרח טעות (צוותים אמיתיים מפצלים ככה כי אנשים שונים עובדים על כל צד), אבל ההחלטה התקבלה לפני שהטיעון הזה עלה. תיעדתי אותה כ-ADR-001 ב-`CONTEXT.md` יחד עם המתח הזה, ורשמתי אותה כשאלה פתוחה. שווה החלטה מודעת, לא ברירת מחדל.

## אב-טיפוס אינטראקטיבי

`prototype/inbox-demo.html` בריפו הזה — קובץ HTML עצמאי אחד (HTML+CSS+JS פשוט, ללא framework, ספריית האנימציה Motion מוטמעת inline) שמדגים את כל הזרימה: מצגת PowerPoint אמיתית (אפיון INBOX) → ניתוח → חיבור ל-Jira → לוח משימות דו-פאנלי עם קישור בין כל משימה למשפט המדויק באפיון שממנו נגזרה. פותח אותו ישירות בדפדפן.

**הערה:** קוד האב-טיפוס הוא כלי הדגמה בלבד, לא בסיס לקוד האמיתי — הוא לא בשפה שנבחרה ל-build (PHP/Python + React). מה שכן רלוונטי משם: הפריסה, הצבעים, מודל האינטראקציה, ומבנה נתוני המשימה/אפיון.

## היסטוריית עדכונים

מסמך זה נכתב לראשונה כתיעוד אישי על תהליך שונה לגמרי (פייפליין AI ב-InManage), ולא עודכן לאורך רוב השיחה שבה המוצר הוגדר מחדש כמערכת גנרית של מצגת-אפיון-לטיקטים. גרסה זו היא הפעם הראשונה שהמסמך תואם את כל ההחלטות בפועל — כולל פיצול שרת/לקוח לשתי משימות, סוג Issue קבוע (`Task`), הציטוט התמידי, ורשימות המשתמשים/סטטוסים/Epics האמיתיות. מכאן והלאה כל שינוי בסשן יתעדכן גם כאן וגם באב-הטיפוס, לא רק באחד מהם.
